node {
    stage(SCM-PROCESS) {
        git branch: 'deploy', credentialsId: '077', url: 'https://github.com/Terraform-Learning/code_terraform.git'
    }
    stage(BUILD-PROCESS) {
        sh ''' terraform init
        terraform apply -auto-approve
        aws s3 ls
        aws s3 cp ./*.tfstate s3://Terraformstorage'''
    }
}